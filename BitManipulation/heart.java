package BitManipulation;

public class heart {

 /*
 
 Meaning:
Create a number whose i-th and j-th bits are set to 1.

 ->   int mask = (1 << i) | (1 << j);


00000100

OR

00010000

------------

00010100
 
 
| HashSet Thinking | Bitmask Formula    | Read It As      |          |
| ---------------- | ------------------ | --------------- | -------- |
| `add(x)`         | `mask              | = (1<<x)`       | Insert x |
| `contains(x)`    | `(mask&(1<<x))!=0` | Is x present?   |          |
| `remove(x)`      | `mask &= ~(1<<x)`  | Delete x        |          |
| Toggle           | `mask ^= (1<<x)`   | Flip x          |          |
| Union            | `mask1  | mask2`   | Combine sets    |          |
| Intersection     | `mask1 & mask2`    | Common elements |          |
| Difference       | `mask1 & (~mask2)` | Remove common   |          |

 
 
 
 

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 */   
    
}
