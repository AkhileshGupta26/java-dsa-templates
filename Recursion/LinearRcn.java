package Recursion;

public class LinearRcn {

    /*
    
    ReturnType solve(Parameter p) {

    // 1. Base Case
    if (smallestProblem)
        return baseAnswer;

    // 2. Ask recursion to solve a smaller problem
    ReturnType smallAnswer = solve(smallerProblem);

    // 3. Use that answer to build the current answer
    ReturnType currentAnswer = combine(currentInput, smallAnswer);

    return currentAnswer;
}
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    */
    
}
