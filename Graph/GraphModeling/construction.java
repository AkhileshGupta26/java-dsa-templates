package Graph.GraphModeling;

public class construction {
/*

#####  The Universal Construction Loop  #####

Map<String, List<String>> graph = new HashMap<>();
Map<String, Integer> indegree = new HashMap<>();

for (int i = 0; i < recipes.length; i++) {

    String node = recipes[i];

    for (String prerequisite : ingredients.get(i)) {

        // prerequisite → node
        graph.computeIfAbsent(prerequisite, k -> new ArrayList<>()).add(node);

        indegree.put(node, indegree.getOrDefault(node, 0) + 1);
    }
}















*/    
    
}
