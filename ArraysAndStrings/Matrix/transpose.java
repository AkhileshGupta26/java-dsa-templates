package ArraysAndStrings.Matrix;

public class transpose {
/*

Template 1 — General Transpose (Most Universal)

int rows = matrix.length;
int cols = matrix[0].length;

int[][] ans = new int[cols][rows];

for (int i = 0; i < rows; i++) {
    for (int j = 0; j < cols; j++) {
        ans[j][i] = matrix[i][j];
    }
}

return ans;




Template 2 — In-place Transpose (Square Matrix Only)

    int n = matrix.length;

    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {

            int temp = matrix[i][j];
            matrix[i][j] = matrix[j][i];
            matrix[j][i] = temp;
        }
    }




















*/    
    
}
