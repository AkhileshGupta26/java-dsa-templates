package ArraysAndStrings.Greedy;

public class sorting {

/*



| Goal                     | Comparator                                                                                                            |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| Ascending                | `Arrays.sort(arr)`                                                                                                    |
| Descending (`Integer[]`) | `Collections.reverseOrder()`                                                                                          |
| 2D by first              | `(a,b)->Integer.compare(a[0], b[0])`                                                                                  |
| 2D by second             | `(a,b)->Integer.compare(a[1], b[1])`                                                                                  |
| Desc first               | `(a,b)->Integer.compare(b[0], a[0])`                                                                                  |
| Desc second              | `(a,b)->Integer.compare(b[1], a[1])`                                                                                  |
| Difference               | `(a,b)->Integer.compare(a[0]-a[1], b[0]-b[1])` *(or compute differences into `int`/`long` first if values are large)* |
| Ratio                    | `Double.compare(...)`                                                                                                 |
| Tie-break                | `Comparator.comparingInt(...).thenComparingInt(...)`                                                                  |
| Safe compare             | `Integer.compare(x, y)`                                                                                               |





































*/    
    
}
