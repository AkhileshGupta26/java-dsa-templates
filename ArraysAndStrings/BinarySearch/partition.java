package ArraysAndStrings.BinarySearch;

public class partition {

/*

IDENIFICATION -> MERGE ARRAY IN: O(log(Math.min(m,n))) instead of O(M+N)

if (Aleft > Bright)
    e = m - 1;
else if (Bleft > Aright)
    s = m + 1;
























*/    
    
}
