package ArraysAndStrings.BinarySearch;

public class infiniteSearchSpace {
/*

Apply when we have infinite search space  like-
1 3 5 7 9 12 15 18 22 27 35 40 50 ...

public int search(int[] arr, int target) {

    int s = 0;
    int e = 1;

    // Phase 1 : Expand Window
    while (arr[e] < target) {

        s = e + 1;
        e = e * 2;
    }

    // Phase 2 : Classical Binary Search
    while (s <= e) {

        int m = s + (e - s) / 2;

        if (arr[m] == target)
            return m;

        if (target < arr[m])
            e = m - 1;
        else
            s = m + 1;
    }

    return -1;
}


























*/    
    
}
