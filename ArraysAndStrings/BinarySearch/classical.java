package ArraysAndStrings.BinarySearch;

public class classical {
/*

The correct definition is:
Binary Search = Search in a Monotonic Search Space

public int binarySearch(int[] nums, int target) {

    int s = 0;
    int e = nums.length - 1;

    while (s <= e) {

        int m = s + (e - s) / 2;

        if (nums[m] == target)
            return m;

        if (target < nums[m])
            e = m - 1;
        else
            s = m + 1;
    }

    return -1;
}



































*/    
    
}
