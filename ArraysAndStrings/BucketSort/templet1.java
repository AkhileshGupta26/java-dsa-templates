package ArraysAndStrings.BucketSort;

public class templet1 {
/*

C->B->F->T


// 1. Count
HashMap<T, Integer> map = new HashMap<>();

// ...

// 2. Bucket
List<T>[] bucket = new ArrayList[MAX + 1];

// 3. Fill
for (var e : map.entrySet()) {

    int key = e.getValue();

    if (bucket[key] == null)
        bucket[key] = new ArrayList<>();

    bucket[key].add(e.getKey());
}

// 4. Traverse
for (int i = MAX; i >= 0; i--) {

    if (bucket[i] == null)
        continue;

    for (T item : bucket[i]) {

        // Do problem-specific work
    }
}
































*/    
    
}
