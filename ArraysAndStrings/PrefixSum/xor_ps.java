package ArraysAndStrings.PrefixSum;

public class xor_ps {
/*

What HashMap stores?

Count Problem
Prefix XOR -> Frequency

Longest Problem
Prefix XOR -> First Index

Same rule as before!


HashMap<Integer,Integer> map=new HashMap<>();

map.put(0,1);

int xor=0;
int ans=0;

for(int x:nums){

    xor ^= x;

    ans += map.getOrDefault(xor ^ k,0);

    map.put(xor,map.getOrDefault(xor,0)+1);
}















*/    
    
}
