package ArraysAndStrings.PrefixSum;

public class templet2 {
/*

#########  Prefix Sum → First Index ########//#endregion

What HashMap stores?
Prefix Sum -> First Index
  
####### TEMPLET ####

HashMap<Integer,Integer> map=new HashMap<>();

map.put(0,-1);

int prefix=0;
int ans=0;

for(int i=0;i<nums.length;i++){

    prefix+=nums[i];

    if(map.containsKey(prefix-k))
        ans=Math.max(ans,i-map.get(prefix-k));

    map.putIfAbsent(prefix,i);
}









































*/    
    
}
