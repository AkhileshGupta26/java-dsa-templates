package MicroTools.Comparator;

public class listSorting {
/*


(1) 1D LIST
Ascending  :Collections.sort(list);
Descending :Collections.sort(list,(a,b)->b-a);

(2)2D LIST SORTING
Collections.sort(list,
        (a,b)->a[0]-b[0]);

(3)List of Objects ⭐⭐⭐⭐⭐
class Job{

    int start;

    int end;

    int profit;
}

Sort : Collections.sort(jobs,
        (a,b)->a.start-b.start);        























*/    
    
}
