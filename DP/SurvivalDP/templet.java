package DP.SurvivalDP;

public class templet {
/*


int solve(i, j) {

    // Invalid
    if (outOfBounds)
        return INF;

    // Destination
    if (destination)
        return Math.max(MIN, END - value);

    // Future Requirement
    int need = minimum(allNextStates);

    // Current Requirement
    return Math.max(MIN,
                    need - value);
}























*/    
    
}
