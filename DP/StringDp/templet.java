package DP.StringDp;

public class templet {
/*


public int solve(int i, String s) {

    // BASE CASE
    if(i == s.length())
        return BASE;

    int ans = INITIAL;

    // Try every possible prefix/substring
    for(int j = i; j < s.length(); j++) {

        // Is s[i...j] a valid choice?
        if(isValid(i, j, s)) {

            // Choose s[i...j]
            int curr = solve(j + 1, s);

            ans = COMBINE(ans, curr);
        }
    }

    return ans;
}













int solve(int i) {

    // BASE
    if(i == n)
        return BASE;

    int ans = INITIAL;

    for(int j = i; j < n; j++) {

        if(valid(i, j)) {

            ans = COMBINE(
                ans,
                solve(j + 1)
            );
        }
    }

    return ans;
}




























*/    
    
}
