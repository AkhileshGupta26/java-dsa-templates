package DP.StringDp;

public class templet2 {
/*

////// For COUNTING
/// 
public int solve(int i, String s) {

    if(i == s.length())
        return 1;

    int ways = 0;

    for(int j = i; j < s.length(); j++) {

        if(isValid(i, j, s)) {

            ways += solve(j + 1, s);
        }
    }

    return ways;
}



solve(i)
   ↓
try j = i → n-1
   ↓
validate s[i...j]
   ↓
take this substring
   ↓
solve(j+1)
   ↓
count / min / max






























*/    
    
}
