package DP.GraphDp;

public class backtrackingDfs {
/*



int[][] dir = {
    {-1,0},
    {1,0},
    {0,-1},
    {0,1}
};

public void solve(int i,int j){

    // Invalid move
    if(i<0 || j<0 || i>=m || j>=n)
        return;

    if(visited[i][j])
        return;

    // Choose
    visited[i][j]=true;

    // Explore
    for(int[] d:dir){

        int nr=i+d[0];
        int nc=j+d[1];

        solve(nr,nc);
    }

    // Undo
    visited[i][j]=false;
}






















*/    
    
}
