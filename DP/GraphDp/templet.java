package DP.GraphDp;

public class templet {
/*




int solve(State current){

    // 1. Already computed?
    if(dp[current] != -1)
        return dp[current];

    // 2. Base answer
    int ans = BASE;

    // 3. Explore every possible next state
    for(State next : neighbours(current)){

        if(valid(next)){

            ans = COMBINE(
                    ans,
                    TRANSITION + solve(next)
                  );
        }
    }

    // 4. Store
    return dp[current] = ans;
}






















*/    
    
}
