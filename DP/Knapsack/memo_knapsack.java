package DP.Knapsack;

public class memo_knapsack {
/*


public int solve(int idx, int W, int[] val, int[] wt) {

    if (idx == wt.length || W == 0)
        return 0;

    if (dp[idx][W] != -1)
        return dp[idx][W];

    int notTake = solve(idx + 1, W, val, wt);

    int take = 0;
    if (wt[idx] <= W)
        take = val[idx] + solve(idx + 1, W - wt[idx], val, wt);

    return dp[idx][W] = Math.max(take, notTake);
}




















*/    
    
}
