package DP.FrequencyAggregationDp;

public class templet {
/*

1. Values are small → int[] freq ⭐

int max = 0;

for(int x : nums)
    max = Math.max(max, x);

int[] freq = new int[max + 1];

for(int x : nums)
    freq[x]++;



2. Values are large → HashMap ⭐

HashMap<Integer, Integer> freq = new HashMap<>();

for(int x : nums){
    freq.put(x, freq.getOrDefault(x, 0) + 1);
}

Process:

for(int x : freq.keySet()){
    int count = freq.get(x);
}






























*/    
    
}
