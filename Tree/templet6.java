package Tree;

public class templet6 {


/*

Template 6 — Backtracking / Path Building


void dfs(TreeNode root, List<Integer> path){

    if(root == null){
        return;
    }

    // choose
    path.add(root.val);

    // leaf / answer condition

    dfs(root.left, path);

    dfs(root.right, path);

    // undo choice
    path.remove(path.size() - 1);
}





















































*/
    
}
