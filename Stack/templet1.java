package Stack;

public class templet1 {

/*

📚 Stack Template 1 — Simple Push Stack

 You only need to remember previous elements.

Instead, ask yourself:
What information do I need to remember for later?
If the answer is:
"The most recent thing."
👉 Think Stack.


Deque<Character> dq = new ArrayDeque<>();

for (char ch : s.toCharArray()) {

    // Process current character (optional)

    dq.push(ch);
}

// Process remaining stack if needed
while (!dq.isEmpty()) {
    char x = dq.pop();
}





Deque<Integer> dq = new ArrayDeque<>();

for (int num : nums) {

    // Process current element (optional)

    dq.push(num);
}

while (!dq.isEmpty()) {
    int x = dq.pop();
}










questions to solve 

Easy (Must Do)

Valid Parentheses	Learn push & pop
Baseball Game	Store previous values
Backspace String Compare	Character stack
Remove All Adjacent Duplicates In String	Push/pop based on top


Medium

Problem	Concept
Simplify Path	Directory stack
Crawler Log Folder	Navigation history
Build an Array With Stack Operations	Stack simulation














*/
    
}
