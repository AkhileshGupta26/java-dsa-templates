package Stack;

public class basics {
/*

But Wait...

Almost nobody uses Stack in modern Java interviews.

Instead use : DEQUE

Deque<Integer> st = new ArrayDeque<>();

because:
->Faster
->No synchronization overhead
->Recommended by Java documentation

 Deque can behave as:
✅ Stack (LIFO)
✅ Queue (FIFO)

A Deque (Double Ended Queue) is like this:

Front ---------------- Back

You can insert/remove from both ends.

push front   ←  [ ]  →  push back

pop front    ←  [ ]  →  pop back


(1) Stack Methods
dq.push(x);
dq.pop();
dq.peek();

These make the deque behave like a stack (LIFO).

(2)Queue Methods
dq.offer(x);
dq.poll();
dq.peek();

These make the deque behave like a queue (FIFO).

(3)Double-Ended Methods (Advanced)

Later, for Monotonic Queue and Sliding Window, we'll use:

dq.offerFirst(x);
dq.offerLast(x);
dq.pollFirst();
dq.pollLast();
dq.peekFirst();
dq.peekLast();






















*/    
    
}
