package Stack;

public class monotonic_index {
 /*
  
 Storing indices is very crucial while solving problems of stk

 Deque<Integer> st = new ArrayDeque<>();
int n = nums.length;
int[] ans = new int[n];

// Initialize default answer if needed
Arrays.fill(ans, -1);

for(int i = 0; i < n; i++){

    while(!st.isEmpty() && CONDITION(nums[st.peek()], nums[i])){

        int idx = st.pop();

        // Fill answer for idx
        ans[idx] = SOMETHING(idx, i);
    }

    st.push(i);
}

// Process remaining indices if needed
while(!st.isEmpty()){
    int idx = st.pop();

    // Default answer
}
 

THIS IS WHY WE POP

We are not removing useless elements.

We are removing elements whose answer has been found.

This is the biggest misunderstanding.
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 */   
    
}
