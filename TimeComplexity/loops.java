package TimeComplexity;

public class loops {
/*


   My Interview Rule (works for almost every loop)
(1)How does the loop variable change?
  i++ → linear.
  i*=2 or i/=2 → logarithmic.
(2)Does the inner loop depend on N or on i?
  Depends on N → multiply complexities.
  Depends on i → add the work across iterations.

  (Q/1) What is time complexity of following code :

        int count = 0;
        for (int i = N; i > 0; i /= 2) {
            for (int j = 0; j < i; j++) {
                count += 1;
            }
        }
   Ans=O(N)     
   
   (Q/2)What is the time complexity of the following code :

        int j = 0;
        for(int i = 0; i < n; ++i) {
            while(j < n && arr[i] < arr[j]) {
                j++;
            }
        }
    Ans=O(N)        





























*/    
    
}
